#include <stdio.h>
#include <string.h>

/**
 * _strlen - The program returns the length of the a string.
 * @s: The string to return its length.
 *
 * Return: The value of the length of a string.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
