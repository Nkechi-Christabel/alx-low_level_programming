#include "main.h"
#include <stdio.h>

/**
 * main - Prints the sum of the multiples of 3 and 5.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int sum = 5 / 2 * (3 + 1024);

	printf("%d\n", sum);

	return (0);
}
